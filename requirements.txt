# The real dependencies. Nothing outside this list is imported by this project.
strands-agents>=1.55.1     # the agent framework
boto3>=1.43.93             # Bedrock provider + credential resolution
flask>=3.1.3               # the dashboard

# Not listed on purpose:
#   strands-agents-tools   prebuilt Strands tools. Fill In defines all six of
#                          its own tools and imports none of these, so adding it
#                          would only be a dependency nobody uses.
#
# Model providers beyond Bedrock are optional extras:
#   pip install "strands-agents[gemini]"      Google Gemini, free tier
#   pip install "strands-agents[anthropic]"   Anthropic API
